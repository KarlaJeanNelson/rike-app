import React from 'react';
import UserHomeButton from '../UI/UserHomeButton';
import LogOutButton from '../UI/LogOutButton';

const UserNavButtons = (props) => (
	<nav>
		<UserHomeButton />
		<LogOutButton />
	</nav>
);


export default UserNavButtons;
